const config = require("../weavedb.config.js")

describe("WeaveDB", () => {
  it("should deploy DB contract", () => {})
  it("should set schemas", () => {})
  it("should set rules", () => {})
  it("should set triggers", () => {})
  it("should set crons", () => {})
  it("should execute queries", () => {})
})
