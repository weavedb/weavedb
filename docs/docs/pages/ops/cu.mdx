# Compute Unit

Clonne the repo.

```bash
git clone https://github.com/weavedb/weavedb.git
cd weavedb npm install && cd hb npm install && cd ../
```

Run.

```bash
yarn cu --wallet ./HyperBEAM/.wallet.json --pid PID
```

Now, a CU should be running at [localhost:6366](http://localhost:6366).