# Validator Node

Clonne the repo.

```bash
git clone https://github.com/weavedb/weavedb.git
cd weavedb npm install && cd hb npm install && cd ../
```

Run.

```bash
yarn validator --wallet ./HyperBEAM/.wallet.json --pid PID
```

